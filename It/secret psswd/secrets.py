import sqlite3
"""log_in = input("Login: ")
passwd = input("Password: ")
connection = sqlite3.connect("/home/roman/Documents/It/secret psswd/Login.db")
crsr = connection.cursor()"""
#sql_username = """ SELECT * FROM lgn WHERE Username = ? AND Password = ? """
"""crsr.execute(sql_username,(log_in,passwd))
lgn = crsr.fetchall()
if lgn:
    pass
else:
    print("Incorect login or password\n")  
connection.commit()
connection.close()"""

